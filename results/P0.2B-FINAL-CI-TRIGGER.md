# Final paired CI trigger

This commit was created after the trusted IWMPROBE workflow gained the P0.2B paired-build job. It forces a pull-request synchronize event on `exp/fastiwm-p0.2b-final` so the matching FASTPROBE image and production-Rev1 auto-detect FujiNet firmware are built together.
