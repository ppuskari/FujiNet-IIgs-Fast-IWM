# P0.2B paired production release trigger

This commit triggers the production release workflow after the responder was changed so the GPIO phase ISR only latches the private request and `systemBus::service()` performs the blocking 2 MHz SPI transmit.

Target hardware: FujiApple production Rev1 using upstream runtime auto-detection. No `REV1DETECT` or other hardware override flags are enabled.
