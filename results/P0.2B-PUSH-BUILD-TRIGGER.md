# P0.2B deterministic build trigger

This commit triggers the main-branch push workflow that explicitly checks out `exp/fastiwm-p0.2b` and builds the matching FASTPROBE ProDOS image plus production-Rev1 auto-detect FujiNet firmware.
